public class NewAccounts {
    
    //method to add 151 accounts
    public static void addAccount() {
        
        Integer maxAcc = 151;
        
        //create a list to add accounts
        List<Account> pokemonAcc = new List<Account>();
        for(Integer i=0; i<maxAcc; i++) {
            
            //create a new account
            Account newAcc = new Account();
            newAcc.Name = 'pokemon ' + i;
            
            pokemonAcc.add(newAcc);
            System.debug(pokemonAcc);
        }
       insert pokemonAcc;
        
    }
    
    public static void removeAccount() {
        
        List<Account> pokemon = [SELECT Name FROM Account WHERE Name LIKE 'pokemon %'];
        delete pokemon;
        
    }
    
    public static void updateContact() {
        
        List<Contact> noEmailphoneC = [SELECT Email, Phone FROM Contact WHERE Email = null OR Phone = null];  
        for (Contact c : noEmailphoneC) {
            c.Email = 'f_name.l_name@companyname.com';
            c.Phone = '(555)555-5555';
            
            System.debug(c);
        }
        update noEmailphoneC;
        
    }
    
    public static void addContact() {
        
        List<Contact> existContact = [SELECT FirstName, LastName FROM Contact WHERE FirstName != 'Bob'];
        
        for (Contact c : existContact) {
            c.FirstName = 'Bob';
        }
        
        for(Integer i=0; i<200; i++) {
            Contact newContact = new Contact();
            newContact.LastName = 'unKnown ' + i;
            
            existContact.add(newContact); 
        }
      
        upsert existContact;

    }
    
}